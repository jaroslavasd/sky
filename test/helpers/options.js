const FLIGHT_PROBLEM = {
  DELAYED: 'del',
  CANCELLED: 'can',
  BOARDING_DENIED: 'dbo',
};

const DELAY_DURATION = {
  LESS_THAN_THREE: 'lt3',
  MORE_THAN_THREE: '3b5',
  MISSED_CONNECTION: 'gt5',
};

module.exports = {
  FLIGHT_PROBLEM,
  DELAY_DURATION,
};
